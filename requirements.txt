anthropic>=0.120.2
pandas>=3.0.5
python-dotenv>=1.2.2
pytest>=9.1.1
# Required by pandas.read_excel / pd.ExcelFile for .xlsx and .xlsm input.
# Without it the XLSX tests importorskip themselves, so CI reports green on
# code that would fail at runtime.
openpyxl>=3.1.0
